<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Tema personalizado para la pantalla de inicio -->
    <style name="CustomLaunchTheme" parent="@android:style/Theme.Light.NoTitleBar">
        <!-- Define el fondo usando el color de splash -->
        <item name="android:windowBackground">@color/ic_launcher_background</item> <!-- Aquí se usa el color o imagen definida -->
        <item name="android:forceDarkAllowed">false</item>
        <item name="android:windowFullscreen">false</item>
        <item name="android:windowDrawsSystemBarBackgrounds">false</item>
        <item name="android:windowLayoutInDisplayCutoutMode">shortEdges</item>
    </style>
</resources>
