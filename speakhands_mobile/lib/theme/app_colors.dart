import 'package:flutter/material.dart';

class AppColors {
  // Colores para el tema claro
  static const lightPrimary = Color(0xFF44CBCC);
  static const lightSecondary = Color(0xFFAEC6CF);
  static const lightAccent = Color(0xFFA0E7E5);
  static const lightBackground = Color(0xFFEFF3FF);
  static const lightForebackground = Color.fromARGB(255, 246, 247, 255);

  // Colores para el tema oscuro
  static const darkPrimary = Color(0xFF5C8DAD);
  static const darkSecondary = Color(0xFF6D9DC5);
  static const darkAccent = Color(0xFF7FBEEB);
  static const darkBackground = Color(0xFF121212);
  static const darkForebackground = Color(0xFF000000);

  // Otros colores
  static const lightText = Color(0xFF2F3A4A);
  static const darkText = Color(0xFFE8F0FE);
  static const errorColor = Color(0xFFB00020);
  static const textPrimary = Color(0xFF2F3A4A);
  static const textSecondary = Color(0xFFBDBDBD);
}
